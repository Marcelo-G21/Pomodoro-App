const MessageOverlay = () => {
  return (
    <div
      className={`fixed inset-0 flex items-center justify-center bg-indigo-700 bg-opacity-50 z-30`}
    >
      <span className="text-4xl md:text-6xl font-bold text-white">
        uwu
      </span>
    </div>
  );
};

export default MessageOverlay;
